import React, { useEffect } from "react";

const LessonPlan = () => {
  useEffect(() => {
    // get preferences and messageId from chrome.local.storage
  });
  return <div>LessonPlan</div>;
};

export default LessonPlan;
